version: 2
jobs:
  test:
    docker:
      - image: cimg/node:16.19.0
    parallelism: 1
    steps:
      - checkout
      - run: npm i
      - run: npm run lint
      - run: npm run compile
      - run: npm run test
workflows:
  version: 2
  test:
    jobs:
      - test
